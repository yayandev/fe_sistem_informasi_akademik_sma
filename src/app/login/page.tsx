export const metadata = {
  title: "Login",
};

import LoginView from "@/components/views/LoginView";

const LoginPage = () => {
  return (
    <>
      <LoginView />
    </>
  );
};

export default LoginPage;
